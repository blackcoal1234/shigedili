# Server-side base URL for the deterministic Python service.
POETRY_AGENT_BACKEND_URL=http://127.0.0.1:8123

# Public same-origin CopilotKit Runtime endpoint exposed by this Next.js app.
NEXT_PUBLIC_COPILOTKIT_RUNTIME_URL=/api/copilotkit
